meta {
  name: 21-invalid-restock
  type: http
  seq: 21
}

post {
  url: {{inventoryUrl}}/products/{{productId}}/restock
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Idempotency-Key: {{runId}}-invalid-restock
}

body:json {
  {
    "movementId": "{{movementId}}",
    "quantity": 0,
    "reason": "INVALID"
  }
}

tests {
  test('Expected HTTP status', () => expect(res.getStatus()).to.equal(400));
}
