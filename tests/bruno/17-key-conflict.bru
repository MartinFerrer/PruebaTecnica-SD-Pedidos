meta {
  name: 17-key-conflict
  type: http
  seq: 17
}

post {
  url: {{inventoryUrl}}/products
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Idempotency-Key: {{runId}}-product
}

body:json {
  {
    "sku": "bruno-{{runId}}",
    "name": "Changed",
    "initialStock": 5
  }
}

tests {
  test('Expected HTTP status', () => expect(res.getStatus()).to.equal(409));
}
